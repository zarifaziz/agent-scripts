# Catastrophic patterns - always blocked

# rm variants
rm -rf ~
rm -rf $HOME
rm -rf /
rm -rf /*
rm -rf ~/*
rm -rf $HOME/*
rm -rf /Users
rm -rf . /
rm -rf ./
rm -r . /
rm -r ./
rm * /

# xargs destructive
xargs rm
xargs -I

# chmod/chown
chmod -R 777 /
chmod -R 777 ~
chown -R * /

# disk destruction
dd if=/dev/zero
dd if=/dev/random
dd if=/dev/urandom
of=/dev/sd
of=/dev/disk
of=/dev/nvme
mkfs.
> /dev/sd

# fork bombs
:(){:|:&};:
:(){ :|:& };:
bomb(){ bomb|bomb& };bomb

# mv dangerous
mv ~ 
mv / 
mv $HOME 
mv . /

# unset/empty var expansion dangers
rm -rf $/
rm -rf ${
:-/}
$UNSET_VAR/

# path traversal
/../../../
../*
